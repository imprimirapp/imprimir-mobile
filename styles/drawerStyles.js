import {StyleSheet} from 'react-native';

const dstyles =  StyleSheet.create({
  container:{
    flex:1
  }
});

const drawerStyles = {
  drawer:{
    shadowColor:'#fff',
    shadowOpacity:0.1,
    shadowRadius: 1,
    width:'100%'
  }
}

export default {
  dstyles,
  drawerStyles
}