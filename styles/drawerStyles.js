import {StyleSheet} from 'react-native';

const styles =  StyleSheet.create({

});

const drawerStyles = {
  drawer:{
    shadowColor:'#fff',
    shadowOpacity:0.1
  }
}

export default {
  styles,
  drawerStyles
}